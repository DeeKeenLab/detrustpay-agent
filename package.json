{
  "name": "detrustpay-agent",
  "version": "0.1.0-alpha.1",
  "private": true,
  "description": "Agent-facing conditional settlement tools for DeTrustPay on Solana",
  "workspaces": [
    "apps/*",
    "examples/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "audit:public": "./scripts/check-public-repo.sh",
    "build": "npm run build --workspace @detrustpay/agent-sdk",
    "test": "npm run test --workspace @detrustpay/agent-sdk"
  }
}
